# This is how we check the type of the variable in Python
a=31
print(type(a)) #this is the type of integer


# to explicitly convert the variable to a different type, we can use the built-in functions like int(), float(), and str().
t = str(a)
print(type (t))